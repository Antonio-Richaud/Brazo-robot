# Instalar solo en el nodo de percepcion. En Jetson Nano/JetPack 4.x puede ser
# necesario compilar librealsense y su wrapper para la version de Python usada.
numpy>=1.24,<3
pyrealsense2>=2.55,<3; platform_machine != "aarch64"
